# C++ 参考代码

## 编译标准

使用 **C++17**，统一编译选项：

```bash
g++ -std=c++17 -O2 -Wall -o <output> <source>.cpp
```

## 目录结构

与 [python/](../python/) 目录完全对称，同一道题共享相同路径和文件名（仅后缀 `.cpp` 与 `.py` 区分）。

## 注意事项

- 代码使用 `long long` 作为默认整数类型，避免 32 位溢出
- 仅在需要极致性能时使用手写优化（如快读快写）
- 建议在理解推导过程后再参考代码
